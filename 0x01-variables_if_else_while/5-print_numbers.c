#include<stdio.h>

/**
 * main - main block
 * Description: prints numbers from 0-9
 * Return: 0
 */

int main(void)
{
	int no = 0;

	while (no < 10)
	{
		printf("%d", no);
		no++;
	}

	return (0);

}
